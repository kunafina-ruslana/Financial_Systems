<!DOCTYPE html>
<html>
<head>
    <title>История операций</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 1000px; margin: 0 auto; padding: 20px; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #f2f2f2; }
        .income { color: #28a745; }
        .expense { color: #dc3545; }
        .transfer { color: #ffc107; }
        .back-link { margin-bottom: 20px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>История операций</h1>
        <div>
            <a href="/wallet">Назад к кошельку</a> |
            <form method="POST" action="/auth/logout" style="display: inline;">
                <button type="submit">Выйти</button>
            </form>
        </div>
    </div>

    <% if (typeof error !== 'undefined') { %>
        <div class="error" style="color: red; margin-bottom: 15px;"><%= error %></div>
    <% } %>

    <% if (transactions.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>Дата</th>
                    <th>Тип</th>
                    <th>Сумма</th>
                    <th>Описание</th>
                    <th>Получатель/Отправитель</th>
                    <th>Статус</th>
                </tr>
            </thead>
            <tbody>
                <% transactions.forEach(transaction => { %>
                    <tr>
                        <td><%= transaction.created_at.toLocaleString() %></td>
                        <td>
                            <% if (transaction.type === 'income') { %>
                                <span class="income">Пополнение</span>
                            <% } else if (transaction.type === 'expense') { %>
                                <span class="expense">Оплата</span>
                            <% } else { %>
                                <span class="transfer">Перевод</span>
                            <% } %>
                        </td>
                        <td><%= transaction.amount %> ₽</td>
                        <td><%= transaction.description %></td>
                        <td>
                            <% if (transaction.type === 'transfer') { %>
                                <%= transaction.recipient %>
                            <% } else if (transaction.type === 'income') { %>
                                -
                            <% } else { %>
                                <%= transaction.recipient || '-' %>
                            <% } %>
                        </td>
                        <td><%= transaction.status %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } else { %>
        <p>Операций пока нет</p>
    <% } %>
</body>
</html>